import {
  HeroContainer,
  HeroTitle,
} from "../../styles/HeroSection.styles";

function HeroSection() {
  return (
    <HeroContainer>
        <HeroTitle>HeroSection</HeroTitle>
    </HeroContainer>
  )
}
export { HeroSection };
